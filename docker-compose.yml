services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data
      - ./backend/static:/app/static
    environment:
      - PYTHONUNBUFFERED=1

  dashboard:
    build: ./dashboard
    ports:
      - "5173:80"
    depends_on:
      - backend
